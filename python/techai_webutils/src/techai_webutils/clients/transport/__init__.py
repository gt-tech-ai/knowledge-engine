"""Server-transport client tier — the runnable RPC server side.

Mirrors Go's ``clients/transport``: the gRPC server runtime lives in ``transport/grpc``
(the client-side channel + interceptor toolkit live in ``clients/rpc``).
"""
